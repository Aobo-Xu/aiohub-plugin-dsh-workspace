# DSH Coding Workspace

This is the independent AIO Hub plugin repository for the DSH coding workspace sidecar. The protocol and runtime implementation are added in subsequent tasks.

For AIO Hub development, create the local plugin link from the AIO repository:

```sh
bun run plugin:dsh:link
```

The link is intentionally local-only and remains ignored by the AIO repository.
